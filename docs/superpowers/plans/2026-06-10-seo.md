# SEO Indexation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Закрыть пробелы индексации лендинга `https://ksenia-kamenskaya.ru` — добавить `robots.txt`, `sitemap.xml`, canonical URLs, verification-метатеги, расширенный JSON-LD (Person + FAQPage) и корректную OG-картинку 1200×630.

**Architecture:** Используем нативный Next.js 16 Metadata API: `app/robots.ts` и `app/sitemap.ts` при `output: 'export'` генерируют статические файлы в `out/`. JSON-LD остаются изолированными компонентами в `components/seo/`. OG-картинка генерируется из `public/images/about.webp` через `sharp` в существующем `scripts/optimize-images.mjs`.

**Tech Stack:** Next.js 16 (App Router, `output: 'export'`), TypeScript, schema.org JSON-LD, sharp 0.33.5. Без автотестов в MVP — проверка через `eslint` + `tsc --noEmit` + ручной smoke в браузере и `curl`.

**Spec:** `docs/superpowers/specs/2026-06-10-seo-design.md`

---

## File Map

**Группа A — OG-картинка (один коммит):**
- Modify `scripts/optimize-images.mjs` — добавить функцию `processOg()` и вызвать её в `main()`
- Generate `public/images/og-cover.webp` (1200×630, sharp.strategy.attention, q80)
- Generate `public/images/og-cover.jpg` (1200×630, mozjpeg q82, fallback для парсеров без webp)

**Группа B — метаданные / robots / sitemap / canonical / verification (один коммит):**
- Create `app/robots.ts`
- Create `app/sitemap.ts`
- Modify `app/layout.tsx` — `alternates.canonical`, `verification.yandex/google`, заменить OG-картинку на 1200×630
- Modify `app/(public)/cookies/page.tsx` — добавить canonical
- Modify `app/(public)/privacy/page.tsx` — добавить canonical
- Modify `app/(public)/offer/page.tsx` — добавить canonical
- Modify `.env.example` — добавить два verification-плейсхолдера

**Группа C — JSON-LD (один коммит):**
- Modify `components/seo/PersonJsonLd.tsx` — расширить (image/description/knowsAbout/areaServed)
- Create `components/seo/FaqJsonLd.tsx`
- Modify `app/(public)/page.tsx` — добавить `<FaqJsonLd />`

**Группа D — manual verify (без коммита):**
- Build, grep `out/` на ключевые метатеги, curl-проверки `robots.txt` и `sitemap.xml`

---

## Task 1: Сгенерировать `og-cover.webp` и `og-cover.jpg` через sharp

**Files:**
- Modify: `scripts/optimize-images.mjs`

Текущий скрипт обрабатывает только responsive-варианты в `public/images/generated/`. Добавим параллельный пайплайн для статичной OG-картинки в `public/images/`.

- [ ] **Step 1: Открыть `scripts/optimize-images.mjs` и найти `main()`**

Текущий main:
```js
async function main() {
    await fs.mkdir(OUT_DIR, { recursive: true });
    for (const source of SOURCES) {
        await processOne(source);
    }
}
```

- [ ] **Step 2: Добавить функцию `processOg()` сразу перед `main()`**

Вставить перед строкой `async function main()`:

```js
async function processOg() {
    const srcPath = path.join(SRC_DIR, 'about.webp');
    const targets = [
        {
            file: 'og-cover.webp',
            encode: (pipeline) => pipeline.webp({ quality: 80 }),
        },
        {
            file: 'og-cover.jpg',
            encode: (pipeline) => pipeline.jpeg({ quality: 82, mozjpeg: true }),
        },
    ];
    for (const { file, encode } of targets) {
        const outPath = path.join(SRC_DIR, file);
        if (await isFresh(srcPath, outPath)) {
            console.log(`skip   ${file}`);
            continue;
        }
        const buffer = await encode(
            sharp(srcPath).resize(1200, 630, {
                fit: 'cover',
                position: sharp.strategy.attention,
            }),
        ).toBuffer();
        await fs.writeFile(outPath, buffer);
        console.log(`wrote  ${file} (${(buffer.length / 1024).toFixed(1)} KB)`);
    }
}
```

Ключевые моменты:
- Источник — `public/images/about.webp` (уже существует, основной портрет).
- `sharp.strategy.attention` — sharp найдёт область наибольшей энтропии (обычно лицо) и центрирует кроп там.
- Файлы пишем в `SRC_DIR` (т.е. `public/images/`), а не в `OUT_DIR`/`generated/` — OG-картинка статична и адресуется метатегом по постоянному пути.
- Используется уже определённая в файле функция `isFresh` для идемпотентности.

- [ ] **Step 3: Вызвать `processOg()` в `main()`**

Заменить:
```js
async function main() {
    await fs.mkdir(OUT_DIR, { recursive: true });
    for (const source of SOURCES) {
        await processOne(source);
    }
}
```
На:
```js
async function main() {
    await fs.mkdir(OUT_DIR, { recursive: true });
    for (const source of SOURCES) {
        await processOne(source);
    }
    await processOg();
}
```

- [ ] **Step 4: Запустить скрипт**

Run: `npm run images`
Expected output (порядок может отличаться): строки `skip   ...` для уже существующих responsive-вариантов, и две новые строки:
```
wrote  og-cover.webp (XX.X KB)
wrote  og-cover.jpg (XX.X KB)
```

- [ ] **Step 5: Проверить файлы на диске**

Run:
```
ls -la public/images/og-cover.webp public/images/og-cover.jpg
identify public/images/og-cover.webp public/images/og-cover.jpg 2>/dev/null || node -e "import('sharp').then(s=>Promise.all(['webp','jpg'].map(e=>s.default(\`public/images/og-cover.\${e}\`).metadata().then(m=>console.log(e, m.width, m.height)))))"
```
Expected: оба файла существуют, размер каждого 1200×630.

- [ ] **Step 6: Коммит группы A**

```bash
git add scripts/optimize-images.mjs public/images/og-cover.webp public/images/og-cover.jpg
git commit -m "$(cat <<'EOF'
chore(images): generate og-cover 1200×630 for social previews

Добавлен пайплайн в scripts/optimize-images.mjs, генерирующий
og-cover.webp и og-cover.jpg из public/images/about.webp.
Используется sharp.strategy.attention — кроп центрируется на
области наибольшей энтропии (обычно лицо). Webp — основной формат,
jpg — fallback для парсеров без поддержки webp (старые версии VK,
некоторые мессенджеры).
EOF
)"
```

---

## Task 2: `app/robots.ts`

**Files:**
- Create: `app/robots.ts`

- [ ] **Step 1: Создать файл `app/robots.ts` со следующим содержимым**

```ts
import type { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
    return {
        rules: {
            userAgent: '*',
            allow: '/',
            disallow: ['/cookies/', '/privacy/', '/offer/'],
        },
        sitemap: 'https://ksenia-kamenskaya.ru/sitemap.xml',
        host: 'https://ksenia-kamenskaya.ru',
    };
}
```

- [ ] **Step 2: Запустить typecheck**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

---

## Task 3: `app/sitemap.ts`

**Files:**
- Create: `app/sitemap.ts`

- [ ] **Step 1: Создать файл `app/sitemap.ts` со следующим содержимым**

```ts
import type { MetadataRoute } from 'next';

export default function sitemap(): MetadataRoute.Sitemap {
    return [
        {
            url: 'https://ksenia-kamenskaya.ru/',
            lastModified: new Date(),
            changeFrequency: 'monthly',
            priority: 1.0,
        },
    ];
}
```

Юр.страницы (`/cookies/`, `/privacy/`, `/offer/`) намеренно отсутствуют — они под `noindex` и не должны индексироваться.

- [ ] **Step 2: Запустить typecheck**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

---

## Task 4: Расширить metadata в `app/layout.tsx`

**Files:**
- Modify: `app/layout.tsx:20-57`

Текущий блок `metadata` нужно дополнить тремя вещами: `alternates.canonical`, `verification`, и заменить OG/Twitter картинку на 1200×630.

- [ ] **Step 1: Найти текущее определение `metadata`**

В `app/layout.tsx:20-57` сейчас:
```ts
export const metadata: Metadata = {
    metadataBase: new URL('https://ksenia-kamenskaya.ru'),
    title: {
        default: 'Ксения Каменская · Психолог · Женские практики',
        template: '%s · Ксения Каменская',
    },
    description:
        'Психолог, специалист по работе с телом и эмоциями. Консультации, длительное сопровождение «Путь к себе», славянская гимнастика «Сила Берегини».',
    keywords: ['психолог', 'женские практики', 'консультация психолога', 'Сила Берегини', 'СПб'],
    authors: [{ name: 'Ксения Каменская' }],
    icons: {
        icon: [{ url: '/icon.svg', type: 'image/svg+xml' }],
    },
    openGraph: {
        type: 'website',
        locale: 'ru_RU',
        url: 'https://ksenia-kamenskaya.ru',
        siteName: 'Ксения Каменская',
        title: 'Ксения Каменская · Психолог · Женские практики',
        description:
            'Психолог, специалист по работе с телом и эмоциями. Консультации, программа «Путь к себе», «Сила Берегини».',
        images: [
            {
                url: '/images/about.webp',
                width: 960,
                height: 1280,
                alt: 'Ксения Каменская — психолог',
            },
        ],
    },
    twitter: {
        card: 'summary_large_image',
        title: 'Ксения Каменская · Психолог · Женские практики',
        description: 'Психология женского тела и проявленности.',
        images: ['/images/about.webp'],
    },
    robots: { index: true, follow: true },
};
```

- [ ] **Step 2: Заменить блок целиком на расширенную версию**

```ts
export const metadata: Metadata = {
    metadataBase: new URL('https://ksenia-kamenskaya.ru'),
    title: {
        default: 'Ксения Каменская · Психолог · Женские практики',
        template: '%s · Ксения Каменская',
    },
    description:
        'Психолог, специалист по работе с телом и эмоциями. Консультации, длительное сопровождение «Путь к себе», славянская гимнастика «Сила Берегини».',
    keywords: ['психолог', 'женские практики', 'консультация психолога', 'Сила Берегини', 'СПб'],
    authors: [{ name: 'Ксения Каменская' }],
    alternates: {
        canonical: '/',
    },
    verification: {
        yandex: process.env.NEXT_PUBLIC_YANDEX_VERIFICATION,
        google: process.env.NEXT_PUBLIC_GOOGLE_VERIFICATION,
    },
    icons: {
        icon: [{ url: '/icon.svg', type: 'image/svg+xml' }],
    },
    openGraph: {
        type: 'website',
        locale: 'ru_RU',
        url: 'https://ksenia-kamenskaya.ru',
        siteName: 'Ксения Каменская',
        title: 'Ксения Каменская · Психолог · Женские практики',
        description:
            'Психолог, специалист по работе с телом и эмоциями. Консультации, программа «Путь к себе», «Сила Берегини».',
        images: [
            {
                url: '/images/og-cover.webp',
                width: 1200,
                height: 630,
                alt: 'Ксения Каменская — психолог',
            },
        ],
    },
    twitter: {
        card: 'summary_large_image',
        title: 'Ксения Каменская · Психолог · Женские практики',
        description: 'Психология женского тела и проявленности.',
        images: ['/images/og-cover.webp'],
    },
    robots: { index: true, follow: true },
};
```

Что изменилось:
1. Добавлен `alternates: { canonical: '/' }` (Next.js резолвит относительно `metadataBase`).
2. Добавлен `verification: { yandex: process.env...., google: process.env.... }`. Если переменная `undefined` — Next.js не рендерит meta-тег. Никаких ручных условий не нужно.
3. `openGraph.images` и `twitter.images` теперь указывают на `/images/og-cover.webp` 1200×630.

- [ ] **Step 3: Запустить typecheck**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

---

## Task 5: Canonical на юр.страницах

**Files:**
- Modify: `app/(public)/cookies/page.tsx:5-8`
- Modify: `app/(public)/privacy/page.tsx:5-8`
- Modify: `app/(public)/offer/page.tsx:5-8`

Каждая юр.страница получает свой canonical. `robots: { index: false }` остаётся.

- [ ] **Step 1: `app/(public)/cookies/page.tsx`**

Найти:
```ts
export const metadata: Metadata = {
    title: 'Политика куки',
    robots: { index: false, follow: false },
};
```

Заменить на:
```ts
export const metadata: Metadata = {
    title: 'Политика куки',
    alternates: { canonical: '/cookies/' },
    robots: { index: false, follow: false },
};
```

- [ ] **Step 2: `app/(public)/privacy/page.tsx`**

Найти:
```ts
export const metadata: Metadata = {
    title: 'Политика обработки персональных данных',
    robots: { index: false, follow: false },
};
```

Заменить на:
```ts
export const metadata: Metadata = {
    title: 'Политика обработки персональных данных',
    alternates: { canonical: '/privacy/' },
    robots: { index: false, follow: false },
};
```

- [ ] **Step 3: `app/(public)/offer/page.tsx`**

Найти:
```ts
export const metadata: Metadata = {
    title: 'Публичная оферта',
    robots: { index: false, follow: false },
};
```

Заменить на:
```ts
export const metadata: Metadata = {
    title: 'Публичная оферта',
    alternates: { canonical: '/offer/' },
    robots: { index: false, follow: false },
};
```

- [ ] **Step 4: Запустить typecheck**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

---

## Task 6: Расширить `.env.example`

**Files:**
- Modify: `.env.example`

- [ ] **Step 1: Текущее содержимое**

```
# Yandex Metrika counter ID — обязательно для прод-сборки.
# Прод-значение задано в .env.production: 109730343.
# Локально оставить пустым — счётчик не активируется (см. lib/analytics/metrika.ts).
NEXT_PUBLIC_YM_ID=
```

- [ ] **Step 2: Дописать в конец файла**

Целевое содержимое:
```
# Yandex Metrika counter ID — обязательно для прод-сборки.
# Прод-значение задано в .env.production: 109730343.
# Локально оставить пустым — счётчик не активируется (см. lib/analytics/metrika.ts).
NEXT_PUBLIC_YM_ID=

# Yandex Webmaster site verification code (meta-tag method).
# Get from https://webmaster.yandex.ru → Settings → Confirm rights.
NEXT_PUBLIC_YANDEX_VERIFICATION=

# Google Search Console site verification code (HTML tag method).
# Get from https://search.google.com/search-console → Settings → Ownership verification.
NEXT_PUBLIC_GOOGLE_VERIFICATION=
```

Прод-значения дозаполнит пользователь в `.env.production` позже, когда получит коды из кабинетов.

---

## Task 7: Lint, typecheck и коммит группы B

- [ ] **Step 1: Запустить полную проверку**

Run: `npm run lint && npx tsc --noEmit`
Expected: 0 ошибок.

- [ ] **Step 2: Просмотр диффа**

Run: `git status && git diff --stat`
Expected: 7 изменений:
```
 .env.example                       | +3 строки комментариев + 2 переменные
 app/(public)/cookies/page.tsx      | +1 строка alternates
 app/(public)/offer/page.tsx        | +1 строка alternates
 app/(public)/privacy/page.tsx      | +1 строка alternates
 app/layout.tsx                     | +alternates, +verification, OG-image swap
 app/robots.ts                      | new file
 app/sitemap.ts                     | new file
```

- [ ] **Step 3: Stage и commit**

```bash
git add app/robots.ts app/sitemap.ts app/layout.tsx \
        app/\(public\)/cookies/page.tsx \
        app/\(public\)/privacy/page.tsx \
        app/\(public\)/offer/page.tsx \
        .env.example
git commit -m "$(cat <<'EOF'
feat(seo): robots, sitemap, canonical, verification, OG 1200×630

Добавлены app/robots.ts и app/sitemap.ts — Next.js Metadata API
генерирует статические robots.txt и sitemap.xml при output: 'export'.
Юридические страницы запрещены и в robots.txt (defence-in-depth) и
через robots meta. Главная и все юр.страницы получили canonical.

В app/layout.tsx добавлены verification-метатеги для Яндекс.Вебмастера
и Google Search Console — рендерятся только при заполненных
NEXT_PUBLIC_YANDEX_VERIFICATION / NEXT_PUBLIC_GOOGLE_VERIFICATION
(коды дозаполнить в .env.production после регистрации в кабинетах).

OG и Twitter картинки заменены с вертикального about.webp (960×1280)
на горизонтальный og-cover.webp (1200×630), что соответствует ожиданиям
соц.сетей и убирает обрезки в превью.
EOF
)"
```

---

## Task 8: Расширить `components/seo/PersonJsonLd.tsx`

**Files:**
- Modify: `components/seo/PersonJsonLd.tsx:3-20`

- [ ] **Step 1: Найти текущее тело компонента**

```tsx
import { BUSINESS } from '@/content/legal/business';

export function PersonJsonLd() {
    const data = {
        '@context': 'https://schema.org',
        '@type': 'Person',
        name: 'Ксения Каменская',
        jobTitle: 'Психолог, специалист по психосоматике и телесной терапии',
        url: BUSINESS.siteUrl,
        email: `mailto:${BUSINESS.email}`,
        sameAs: [
            'https://t.me/xenia_kamensky',
            'https://t.me/kmensky_case',
        ],
        worksFor: {
            '@type': 'Organization',
            name: BUSINESS.name,
            taxID: BUSINESS.inn,
        },
    };
    return (
        <script
            type="application/ld+json"
            dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
        />
    );
}
```

- [ ] **Step 2: Заменить весь файл на расширенную версию**

```tsx
import { BUSINESS } from '@/content/legal/business';

export function PersonJsonLd() {
    const data = {
        '@context': 'https://schema.org',
        '@type': 'Person',
        name: 'Ксения Каменская',
        jobTitle: 'Психолог, специалист по психосоматике и телесной терапии',
        description:
            'Психолог. Консультации, длительное сопровождение «Путь к себе», славянская гимнастика «Сила Берегини».',
        image: `${BUSINESS.siteUrl}/images/about.webp`,
        url: BUSINESS.siteUrl,
        email: `mailto:${BUSINESS.email}`,
        knowsAbout: ['Психология', 'Психосоматика', 'Телесная терапия', 'Женские практики'],
        areaServed: {
            '@type': 'Country',
            name: 'Россия',
        },
        sameAs: [
            'https://t.me/xenia_kamensky',
            'https://t.me/kmensky_case',
        ],
        worksFor: {
            '@type': 'Organization',
            name: BUSINESS.name,
            taxID: BUSINESS.inn,
        },
    };
    return (
        <script
            type="application/ld+json"
            dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
        />
    );
}
```

Добавлены: `description`, `image` (абсолютный URL через `BUSINESS.siteUrl`), `knowsAbout` (4 тематических термина), `areaServed` (Россия).

- [ ] **Step 3: Запустить typecheck**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

---

## Task 9: Новый компонент `components/seo/FaqJsonLd.tsx`

**Files:**
- Create: `components/seo/FaqJsonLd.tsx`

- [ ] **Step 1: Создать файл со следующим содержимым**

```tsx
import { faq } from '@/content/home';

export function FaqJsonLd() {
    const data = {
        '@context': 'https://schema.org',
        '@type': 'FAQPage',
        mainEntity: faq.map((item) => ({
            '@type': 'Question',
            name: item.question,
            acceptedAnswer: {
                '@type': 'Answer',
                text: item.answer,
            },
        })),
    };
    return (
        <script
            type="application/ld+json"
            dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
        />
    );
}
```

`FAQItem` в `content/home.ts:41-45` уже имеет поля `question: string` и `answer: string` — маппинг прямой.

- [ ] **Step 2: Запустить typecheck**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

---

## Task 10: Подключить `<FaqJsonLd />` на главной

**Files:**
- Modify: `app/(public)/page.tsx:5-14`

- [ ] **Step 1: Найти импорты и JSX**

Текущий файл (целиком):
```tsx
import { About } from '@/components/sections/About';
import { Cases } from '@/components/sections/Cases';
import { FAQ } from '@/components/sections/FAQ';
import { Hero } from '@/components/sections/Hero';
import { PersonJsonLd } from '@/components/seo/PersonJsonLd';
import { Services } from '@/components/sections/Services';
import { WorkAreas } from '@/components/sections/WorkAreas';
import { Offer } from '@/components/sections/Offer';
import { cases, faq, qualifications, services, workAreas } from '@/content/home';

export default function HomePage() {
    return (
        <>
            <PersonJsonLd />
            <Hero />
            <About qualifications={qualifications} />
            <WorkAreas items={workAreas} />
            <Cases items={cases} />
            <Services items={services} />
            <FAQ items={faq} />
            <Offer />
        </>
    );
}
```

- [ ] **Step 2: Добавить импорт `FaqJsonLd` и рендер компонента**

Заменить весь файл на:
```tsx
import { About } from '@/components/sections/About';
import { Cases } from '@/components/sections/Cases';
import { FAQ } from '@/components/sections/FAQ';
import { Hero } from '@/components/sections/Hero';
import { FaqJsonLd } from '@/components/seo/FaqJsonLd';
import { PersonJsonLd } from '@/components/seo/PersonJsonLd';
import { Services } from '@/components/sections/Services';
import { WorkAreas } from '@/components/sections/WorkAreas';
import { Offer } from '@/components/sections/Offer';
import { cases, faq, qualifications, services, workAreas } from '@/content/home';

export default function HomePage() {
    return (
        <>
            <PersonJsonLd />
            <FaqJsonLd />
            <Hero />
            <About qualifications={qualifications} />
            <WorkAreas items={workAreas} />
            <Cases items={cases} />
            <Services items={services} />
            <FAQ items={faq} />
            <Offer />
        </>
    );
}
```

- [ ] **Step 3: Запустить typecheck**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

---

## Task 11: Lint и коммит группы C

- [ ] **Step 1: Полная проверка**

Run: `npm run lint && npx tsc --noEmit`
Expected: 0 ошибок.

- [ ] **Step 2: Просмотр диффа**

Run: `git diff --stat`
Expected: 3 изменения:
```
 app/(public)/page.tsx                | +2 строки
 components/seo/FaqJsonLd.tsx         | new file
 components/seo/PersonJsonLd.tsx      | +description/image/knowsAbout/areaServed
```

- [ ] **Step 3: Stage и commit**

```bash
git add components/seo/PersonJsonLd.tsx components/seo/FaqJsonLd.tsx app/\(public\)/page.tsx
git commit -m "$(cat <<'EOF'
feat(seo): расширить Person JSON-LD + добавить FAQPage schema

PersonJsonLd получил поля description, image, knowsAbout, areaServed —
полнее карточка эксперта в Knowledge Graph.

Новый FaqJsonLd генерирует schema.org/FAQPage из существующей секции
FAQ (content/home.ts). Даёт шанс на rich snippet с раскрытыми
вопросами в Яндекс/Google SERP — реальный CTR-буст без правки контента.
EOF
)"
```

---

## Task 12: Manual verification

Никакого коммита здесь нет — это проверка результата.

- [ ] **Step 1: Чистая прод-сборка**

Run: `rm -rf .next out && npm run build`
Expected: успешная сборка. В выводе должны быть `Generating static pages` и упоминание `.env.production` (NEXT_PUBLIC_YM_ID инлайнится).

- [ ] **Step 2: Проверка артефактов в `out/`**

Run:
```
ls out/robots.txt out/sitemap.xml out/index.html out/cookies/index.html out/privacy/index.html out/offer/index.html
```
Expected: все шесть файлов существуют.

- [ ] **Step 3: Проверка `robots.txt`**

Run: `cat out/robots.txt`
Expected (порядок строк может отличаться по версии Next.js):
```
User-Agent: *
Allow: /
Disallow: /cookies/
Disallow: /privacy/
Disallow: /offer/

Host: https://ksenia-kamenskaya.ru

Sitemap: https://ksenia-kamenskaya.ru/sitemap.xml
```

- [ ] **Step 4: Проверка `sitemap.xml`**

Run: `cat out/sitemap.xml`
Expected: валидный XML c корневым `<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">` и одной записью:
```xml
<url>
  <loc>https://ksenia-kamenskaya.ru/</loc>
  <lastmod>2026-06-10T...Z</lastmod>
  <changefreq>monthly</changefreq>
  <priority>1</priority>
</url>
```

- [ ] **Step 5: Проверка canonical и OG-картинки на главной**

Run:
```
grep -o 'rel="canonical"[^>]*' out/index.html
grep -o 'property="og:image"[^>]*' out/index.html
```
Expected:
- canonical содержит `https://ksenia-kamenskaya.ru` (без слэша или со слэшом — оба варианта корректны для Next.js)
- og:image указывает на `/images/og-cover.webp`

- [ ] **Step 6: Проверка JSON-LD на главной**

Run: `grep -c 'application/ld+json' out/index.html`
Expected: `2` (Person + FAQPage).

Run: `grep -oE 'FAQPage|Person' out/index.html | sort -u`
Expected:
```
FAQPage
Person
```

- [ ] **Step 7: Проверка отсутствия verification-метатегов без ENV**

Run:
```
grep -c 'yandex-verification' out/index.html
grep -c 'google-site-verification' out/index.html
```
Expected: `0` и `0` (оба, потому что `NEXT_PUBLIC_YANDEX_VERIFICATION` и `..._GOOGLE_VERIFICATION` пустые/отсутствуют).

- [ ] **Step 8: Проверка появления verification при заполненном ENV**

Run:
```
NEXT_PUBLIC_YANDEX_VERIFICATION=test-yandex NEXT_PUBLIC_GOOGLE_VERIFICATION=test-google npm run build
grep -o 'yandex-verification[^>]*' out/index.html
grep -o 'google-site-verification[^>]*' out/index.html
```
Expected: оба meta-тега присутствуют со значениями `test-yandex` и `test-google` соответственно.

Затем восстановить чистую сборку:
```
rm -rf .next out && npm run build
```

- [ ] **Step 9: Проверка canonical на юр.странице**

Run: `grep -o 'rel="canonical"[^>]*' out/cookies/index.html`
Expected: содержит `/cookies` (со слэшом или без — оба варианта).

Run: `grep -o 'name="robots"[^>]*' out/cookies/index.html`
Expected: содержит `noindex` и `nofollow`.

- [ ] **Step 10: Smoke-тест в браузере**

Запустить локальный сервер (порт 3000 может быть занят — `serve` подберёт другой):
```
npx serve out
```

Открыть `http://localhost:<port>/` в инкогнито:
1. View Source → присутствуют `<link rel="canonical">`, `<meta property="og:image" content="...og-cover.webp">`, два `<script type="application/ld+json">`.
2. Скопировать содержимое первого `<script>` (Person JSON-LD) в https://validator.schema.org/ → валидно.
3. Скопировать содержимое второго `<script>` (FAQ) → валидно.

- [ ] **Step 11: Если что-то не работает**

Любой сбой → остановиться, доложить пользователю. Не пытаться чинить обходным путём. Возможные причины: устаревшая версия `next` без поддержки `app/robots.ts` (требуется ≥ 13.3), забыли вызвать `npm run images` после Task 1, и т.д.

---

## Follow-up (out-of-scope этого плана)

После деплоя на Beget:
1. Зарегистрировать сайт в Яндекс.Вебмастере → получить yandex-verification код → положить в `.env.production`.
2. Зарегистрировать сайт в Google Search Console → получить google-site-verification код → положить в `.env.production`.
3. Пересобрать и задеплоить.
4. В обоих кабинетах подать `sitemap.xml` для ускорения обхода.
5. Через 2–4 недели — мониторинг позиций по запросам «психолог СПб», «психосоматика», «телесная терапия» и т.д.

---

## Done criteria

- [ ] Три коммита в `main`: «og-cover», «robots/sitemap/canonical/verification», «JSON-LD».
- [ ] `npm run lint && npx tsc --noEmit` — чисто.
- [ ] `npm run images` — генерирует `og-cover.webp` и `og-cover.jpg` идемпотентно.
- [ ] `npm run build` — успех. В `out/` присутствуют `robots.txt` и `sitemap.xml` с правильным доменом.
- [ ] Smoke-чек-лист Task 12 — все ✅.
- [ ] `git status` — working tree clean.
